
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Blog Sayfası</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            font-family: Arial, sans-serif;
        }

        .blur-overlay {
            backdrop-filter: blur(5px);
            background-color: rgba(255, 255, 255, 0.5);
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1000;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .popup-content {
            text-align: center;
            background-color: white;
            padding: 20px;
            border: 1px solid #ddd;
        }

        .popup-button {
            display: inline-block;
            padding: 10px 20px;
            margin: 5px;
            font-size: 14px;
            cursor: pointer;
            border: none;
            border-radius: 4px;
            color: #fff;
            background-color: orange;
        }

            .popup-button:hover {
                background-color: #0056b3;
            }
    </style>
</head>

<body>
    <div class="blur-overlay">
        <div class="popup-content">
            <h2>Giriş yapmanız gerekmektedir.</h2>
            <p>Blogları okuyabilmek için lütfen giriş yapın veya kayıt olun.</p>
            <a href="/Auth/Login" class="popup-button">Giriş Yap</a>
            <a href="/Auth/Register" class="popup-button">Kayıt Ol</a>
        </div>
    </div>

    <!-- Buradan itibaren mevcut sayfa içeriğiniz gelecek -->
    <section class="blog-posts">
        <!-- Sayfa içeriği buraya eklenecek -->
    </section>

    <!-- Bootstrap JS ve jQuery -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
